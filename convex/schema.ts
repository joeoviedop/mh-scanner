import { defineSchema } from "convex/server";

// MVP Fase 0: esquema mínimo para habilitar Convex en el proyecto.
// Las tablas completas se implementarán en fases siguientes.
const schema = defineSchema({});

export default schema;
